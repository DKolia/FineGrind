import React from ‘react’;
// import { Marker } from “react-google-maps”


const Pins = (props) => {
    console.log("Pins is being used")
    return(
        "hey"
        // <Marker position={{ [25].location.lat, [25].location.lng }} />
        // console.log([25].location.lat);
    )

}


export default Pins
